source "https://rubygems.org"

# Engine's runtime deps come from the gemspec. This Gemfile only adds
# what's needed to develop and test the engine in isolation. When the
# engine is consumed as a path-gem from the host fobizz platform, the
# host's Gemfile.lock supplies everything.
gemspec

gem "rails", ">= 8.1.3"
gem "puma"
gem "rspec-rails"
gem "standard"
